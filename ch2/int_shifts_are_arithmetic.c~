#include <stdio.h>

int main(){
	int x = -1;
	if ((x>>1)&0b10000000){
		printf("Your machine's right shifts are arithmetic\n");
	} else {
		printf("Your machine's right shifts are not arithmetic.\n");
	}
}
